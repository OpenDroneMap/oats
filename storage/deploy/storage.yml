---
# Deploy the OATS artifact store. See ../README.md.
#
#   ansible-galaxy collection install -r requirements.yml
#   ansible-playbook -i inventory.yml storage.yml
- name: OATS artifact store
  hosts: all
  become: true
  roles:
    - oats_storage
