---
# Copy to inventory.yml (git-ignored). One host: the VM running CI, Garage
# and the viewer.
all:
  hosts:
    oats-store:
      ansible_host: oats-vm.lan
      ansible_user: michael

      # Passed through from the Proxmox host; unset skips all disk handling.
      # WARNING: an unformatted device is formatted.
      # oats_data_device: /dev/sdb

      # Served under this name; the bucket gains a matching alias.
      # oats_public_hostname: oats-store.example.com

      # Write the nginx vhost. False when the proxy is managed elsewhere.
      # oats_nginx_manage: true
